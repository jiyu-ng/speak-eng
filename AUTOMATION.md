# 스픽메이트 🗣 — 자동 기능개선 가이드

스픽메이트는 대표님 **본인용** AI 영어 스피킹 연습 앱(Vite + React + Supabase, GitHub Pages `jiyu-ng.github.io/speak-eng`).
키리의 로테이션 자동개선 크론이 이 파일을 기준으로 **작은 개선 1건**을 골라 구현한다.

## 성격·우선순위 (⚠️ 본인용 앱)

- 광고수익·SEO가 목적이 아님 → **UX·안정성·품질**이 우선.
- 우선순위: **UI/UX · 접근성 · 성능 · 버그 수정 · 코드품질 · 모바일 대응**.
- ⚠️ **핵심 기능 로직은 자동으로 건드리지 않는다** — 회화/음성 처리, Supabase 스키마·쿼리, 외부 API·프롬프트, 인증 흐름. 잘못 손대면 앱이 먹통 되고 티가 크게 남.

## ✅ 자동 머지 OK (빌드 통과 + 기존 기능 안 깨짐)

작고 안전한 개선만:
- **UI/스타일**: 레이아웃 정리·간격·반응형(모바일), 다크/라이트 대비, 버튼·입력 상태
- **접근성**: aria-label, 포커스, 키보드, 색 대비, 시맨틱 태그
- **UX 소품**: 로딩 스피너·스켈레톤, 빈 상태·에러 메시지 문구, 버튼 비활성 처리 (기능 로직 변경 없이 '표시'만)
- **성능**: 불필요 리렌더 정리, 이미지·번들 소소한 최적화, lazy import
- **코드품질**: 죽은 코드 제거, 명확한 네이밍, 주석, 린트/포맷, 의존성 **패치**

**공통 조건**: `npm run build` 통과 + **기존 기능이 그대로 동작**(회화·저장·로그인 등 안 깨짐) + 변경 범위 작음. 하나라도 애매하면 리뷰行.

## 🙋 리뷰行 (머지 X, PR 열어두고 DM 보고)

- 기능 로직·상태관리 흐름 변경, 새 기능/화면 추가
- Supabase 스키마·쿼리·RLS, API·프롬프트·음성 처리 변경
- 인증/결제/환경변수, 의존성 major 업데이트, 빌드·배포 설정
- 디자인 인상이 바뀌는 변화
- **조금이라도 앱 동작에 영향 가면 전부 리뷰行** (기본값)

## 작업 흐름

1. `git checkout main && git pull`
2. 개선 1건 구현 + `npm run build` 통과 확인 (가능하면 핵심 화면 렌더/동작도 눈으로 점검)
3. `auto/feat-<slug>` 브랜치 커밋·push, `gh pr create`
4. '빌드 통과 + 기존 기능 안 깨짐 + 작은 범위'면 자동머지, 아니면 리뷰行 → DM 보고
5. 마땅한 개선거리 없으면 조용히 패스(보고 생략)

## ROADMAP (개선 백로그 — 처리하면 [x], 새 아이디어 추가)

- [ ] 로딩/에러/빈 상태 UI 정리(회화 대기·네트워크 실패 등 '표시'만)
- [ ] 모바일 반응형·버튼 터치 영역·입력 UX 점검
- [ ] 접근성: 포커스 순서, aria, 색 대비, 키보드 조작
- [ ] 불필요한 리렌더·번들 최적화(기능 불변)
- [ ] 코드 정리: 컴포넌트 분리(순수 UI 한정), 죽은 코드·경고 제거
- [ ] 파비콘·메타·타이틀 등 기본 마감
